import { ApplicationService } from './application-service';

describe('ApplicationService', () => {
  it('should create an instance', () => {
    expect(new ApplicationService()).toBeTruthy();
  });
});
